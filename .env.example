# Firebase Configuration (Public - used by client)
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# ... these are actually in firebase-applet-config.json

# Firebase Admin (Secret - used by server)
# In this environment, we use the default projectId from config
PROJECT_ID=gen-lang-client-0351911300
